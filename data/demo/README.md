# Synthetic demonstration data

Every value in this directory is fabricated solely to test software paths.
Names are generic and are not aliases for real locations. Capacities, costs,
times and scores must not be cited as empirical facts.

The CSV timestamps are ISO 8601 UTC. Canonical flow IDs are unique so the quality
gate can detect accidental reuse when fixtures are combined.
